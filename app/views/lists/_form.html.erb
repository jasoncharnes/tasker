<div class="card">
  <div class="card-header">
    New list
  </div>
  <div class="card-body">
    <%= bootstrap_form_with model: list do |form| %>
      <%= form.text_field :name %>
      <%= form.primary %>
    <% end %>
  </div>
</div>
